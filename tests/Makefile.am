check_PROGRAMS = c0
TESTS = $(check_PROGRAMS)

AM_DEFAULT_SOURCE_EXT = .cc
AM_CPPFLAGS += $(LLVM_CPP) -I$(top_srcdir)
AM_CXXFLAGS += $(LLVM_CXX)
AM_LDFLAGS = $(LLVM_LD)
LDADD = ../libvalet.a $(LLVM_LIBS) $(GTEST_LIBS)
